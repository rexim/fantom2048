using gfx

class AppearingCellAnimation: IAnimation {
    override Void nextFrame(Int delay) {
        
    }

    override Void renderFrame(Graphics g) {
        
    }

    override Bool isOver() {
        return false;
    }
}